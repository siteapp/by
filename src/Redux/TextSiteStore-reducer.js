import Logo from "../logo.png";

let initialState = {
    _MetaTitle: '🎉 ᐅ Aliaksandr Kavaliou',
    _Language: 'en',
    _Logo: Logo,
    _TopHEaderBar: 'open',
    _CloseTopHeaderBar: 'closeTopBar',
}

const TextSiteStoreReducer = (store = initialState, action) => {

    return store;
}

export default TextSiteStoreReducer;